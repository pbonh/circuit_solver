[package]
name = "circuit_solver_delta_py"
version = "0.1.0"
edition = "2024"

[lib]
name = "circuit_solver_delta_py"
crate-type = ["cdylib"]

[dependencies]
circuit_solver_delta = { path = ".." }
pyo3 = { version = "0.25", features = ["extension-module", "abi3-py38"] }
numpy = "0.25"
