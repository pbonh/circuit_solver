# Architecture (C4)

Model source: `model.json` (validated with `c4.py validate`).

## System Context (Level 1)
<!-- Paste the output of: c4.py render model.json --level context -->
```mermaid
```

## Containers (Level 2)
<!-- Paste the output of: c4.py render model.json --level container -->
```mermaid
```

## Components (Level 3, optional)
<!-- Only for containers this change reshapes:
     c4.py render model.json --level component --system <id> -->

## Notes
<!-- Key technology choices and links to the ADRs (decisions/) that justify them. -->
-
